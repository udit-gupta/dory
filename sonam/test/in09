/*
 *   return stmt 
 *
 *
 */

   class CString;

    event open (string a1 , int a2 , unsigned int a3);

    int i = 2 ;
    int j = 3 ;
    int k = 4.0 ;

    byte b ;
    string s ;

	void hh(int z);

    int g(int x, double y, CString u) {
         int a,b,c;
         return 1;
   };

   int f(){
 	int a,b;
        return ((a+b));

  };


    open(file, flags, mode) -> {
                     
                     if ((i < j) || (j > k)) {
                        i = (23 * (-i)/j);
                    }
                    else ;
		 	
  


    };
   
